#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

struct node {
       int data;
struct node *link;
};

void printlist(struct node* head)
{

struct head = (struct node *)malloc(sizeof(struct node));


