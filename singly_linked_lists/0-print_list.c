#include <stdio.h>
#include <stdlib.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

struct node {
       int data;
struct node *link;
}
struct node *head = NULL
head = (struct node *)malloc(sizeof(struct node));
head - > = 0;


