#include <stdio.h>
#include "variadict_fuctions.h"
#include <stdarg.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int sum_them_all(const unsigned int n, ...)

{
	int i ,sum;
		sum = 0;
	printf("%d\n, sam);
	if n == 0
		Return = 0 
}
