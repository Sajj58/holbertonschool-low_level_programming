#include <stdio.h>

#include <stdarg.h>
#include "variadict_fuctions.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int sum_them_all(const unsigned int n, ...)

{
	va_list args;
	int i ,sum;
		sum = 0;
	printf("%d\n, sam);
	if n == 0
		Return = 0
